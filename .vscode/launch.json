{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: dev:backend",
            "request": "launch",
            "command": "npm run dev:backend",
            "cwd": "${workspaceFolder}",
        },
    ]
}